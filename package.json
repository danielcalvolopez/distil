{
  "name": "distil",
  "version": "0.0.1",
  "description": "Mine Claude Code session transcripts for corrections and repeated patterns; propose CLAUDE.md rules and skills. Proposes, never acts.",
  "type": "module",
  "bin": { "distil": "./bin/distil.js" },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
